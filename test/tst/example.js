/**
 * Test for nothing...
 */

/* global describe it */

const assert = require('assert');

describe('We want to test', () => {
  it('It should have an assert', () => {
    assert.equal(true, true);
  });
});
